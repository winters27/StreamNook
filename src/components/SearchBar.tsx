const SearchBar = () => {
  return (
    <div className="p-3 bg-secondary backdrop-blur-md border-b border-borderSubtle flex items-center justify-end gap-2">
      {/* Search bar content can be added here in the future */}
    </div>
  );
};

export default SearchBar;
