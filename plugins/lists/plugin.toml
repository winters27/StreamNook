id          = "app.streamnook.lists"
name        = "Lists"
version     = "1.0.0"
author      = "StreamNook"
tier        = "A"
description = "User-curated reference lists at hand while you watch: usernames, commands, titles. Floating panel, popout window, mod-logs dock, palette and hotkey access."
host_min    = "7.8.6"

[runtime]
kind  = "ui"
entry = "dist/main.js"
